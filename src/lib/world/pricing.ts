/**
 * Prices that both the server (charging) and the UI (explaining) need.
 * Kept free of server-only imports so a client component can read them.
 */

/** Restyling a whole district changes far more than adding one object. */
export const ZONE_THEME_COST_CREDITS = 15;
